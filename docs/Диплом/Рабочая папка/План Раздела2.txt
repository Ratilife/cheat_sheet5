2.1. Постановка задачи и функциональные требования к программному средству (это ТЗ)
2.2. Архитектура и структура программного средства (описание продукта, UML диаграммы, структура БД и т.д.)
2.3. Алгоритмическое обеспечение программного средства (описание модулей и принципов работы, возможно с вырезками кода и скринами)
2.4. Тестирование, оценка эффективности и экономическое обоснование разработки (описание результатов тестов,
возможно обоснование с педагогической или экономической стороны, хотя экономику тут вы врядли сможете подтянуть,
но зато можете точно описать результаты нагрузочного и пользовательского теста)





Рекомендации по пункту 2.2 «Архитектура и структура программного средства»:

## Структура пункта 2.2

### 2.2.1. Общее описание программного средства
- Назначение: кроссплатформенное средство для организации и управления учебными материалами и фрагментами кода для IT-специалистов
- Основные функции: стартовая панель, редактор файлов, система управления файлами, парсинг и подсветка синтаксиса
- Целевая аудитория: IT-студенты и начинающие разработчики

### 2.2.2. Архитектура приложения
- Архитектурный стиль: модульная архитектура с разделением на слои
- Основные слои:
  - Presentation Layer (UI) — `src/ui/`, `src/start_panel/views/`
  - Business Logic Layer — `src/managers/`, `src/controllers/`, `src/editor/`
  - Data Access Layer — `src/models/`, `src/parsers/`
  - Infrastructure Layer — `src/utils/`, `src/observers/`
- Используемые паттерны:
  - MVVM (стартовая панель)
  - Factory (создание редакторов)
  - Observer (отслеживание изменений файлов)
  - Strategy (разные типы редакторов)

### 2.2.3. UML-диаграммы
- Диаграмма компонентов: основные модули и их взаимодействие
- Диаграмма классов: ключевые классы (`EditorManager`, `FileParserService`, `STMDFileTreeModel`, `ButtonViewModel`)
- Диаграмма последовательности: сценарии (открытие файла, парсинг, создание кнопки)
- Диаграмма развертывания: компоненты и их размещение

### 2.2.4. Структура данных и хранение
- Формат хранения: файловая система + JSON-файлы (БД не используется)
- Структура данных:
  - `for_program/buttons.json` — конфигурация кнопок
  - `for_program/saved_files.json` — реестр внешних файлов
  - `bookmarks/` — рабочие файлы пользователя (ST/MD)
  - `root_folder_path.json` (в домашней директории) — конфигурация корневой папки
- Модели данных:
  - `STMDFileTreeItem` — элемент дерева файлов
  - `STMDFileTreeModel` — модель для отображения в Qt
  - `ButtonListModel` — модель для стартовой панели

### 2.2.5. Структура модулей приложения
- `src/ANTLR4/` — грамматики для парсинга (ST, MD, Python, Java, C#, 1C)
- `src/parsers/` — парсеры файлов и система кэширования
- `src/editor/` — система редакторов (ST, Markdown, Plain Text)
- `src/managers/` — менеджеры (файлов, UI, дерева, вкладок)
- `src/models/` — модели данных
- `src/ui/` — пользовательский интерфейс
- `src/start_panel/` — стартовая панель (MVVM)

### 2.2.6. Технологический стек
- Язык: Python 3
- GUI: PySide6 (Qt6)
- Парсинг: ANTLR4
- Подсветка синтаксиса: Pygments
- Тестирование: Pytest
- Хранение: JSON, файловая система

### 2.2.7. Взаимодействие компонентов
- Поток данных: UI → Managers → Parsers → Cache → Models
- Система кэширования: `ContentCache` и `MetadataCache` для оптимизации
- Фоновый парсинг: `BackgroundParser` для асинхронной обработки

---

## Что включить в текст
1. Описание архитектуры с обоснованием выбора модульной структуры
2. UML-диаграммы (минимум 2–3): компонентов, классов, последовательности
3. Описание структуры данных: формат JSON-файлов, модель дерева файлов
4. Схему взаимодействия компонентов (можно текстом или диаграммой)
5. Обоснование технологий: почему PySide6, ANTLR4, JSON вместо БД

## Что не нужно
- Подробное описание каждого класса (это для кода)
- Детали реализации алгоритмов (это для других разделов)
- Полный список всех файлов (достаточно основных модулей)



Предлагаемая структура пункта 2.2:
2.1. Архитектура приложения
2.1.1. Архитектурный стиль
2.1.2. Структура слоев приложения
2.1.3. Используемые паттерны проектирования
2.2.2. Структура данных и хранение информации
2.2.3. Взаимодействие компонентов системы
2.2.4. Технологический стек
2.2.5. Структура модулей приложения
2.2.6. Заключение по разделу

План пункта 2.3. Алгоритмическое обеспечение программного средства
2.3.1. Общая структура алгоритмического обеспечения
    -Архитектура алгоритмического обеспечения (распределение алгоритмов по слоям)
    -Принципы организации алгоритмов в модулях
    -Взаимодействие алгоритмов между слоями
    -Диаграмма зависимостей алгоритмов
2.3.2. Система парсинга ANTLR4
    2.3.2.1. Грамматика Structured Text (ST)
        -Описание грамматики ST-файлов
        -Структура грамматического файла (.g4)
        -Правила лексического и синтаксического анализа

    2.3.2.2. Генерация парсеров
        -Процесс генерации лексеров и парсеров из грамматики
        -Структура сгенерированных классов (Lexer, Parser, Listener)
        -Алгоритм работы ANTLR4 генератора

2.3.3. Система кэширования
    2.3.3.1. Алгоритм LRU кэширования (ContentCache)
        -Принцип работы алгоритма Least Recently Used
        -Структура данных для хранения кэша
        -Алгоритм вытеснения элементов при превышении лимита
        -Потокобезопасность и синхронизация
    2.3.3.2. Алгоритм TTL кэширования (MetadataCache)
        -Принцип работы Time To Live
        -Алгоритм проверки актуальности данных
        -Механизм автоматической инвалидации записей
        -Проверка изменений файлов на диске
2.3.4. Алгоритмы парсинга файлов
    2.3.4.1. Парсинг ST-файлов
        -Алгоритм использования ANTLR4 парсера
        -Обработка синтаксического дерева через Listener
        -Извлечение структуры (файлы, папки, шаблоны)
        -Преобразование результатов парсинга в структуру данных приложения
    2.3.4.2. Парсинг Markdown-файлов
        -Алгоритм анализа Markdown-разметки
        -Извлечение иерархической структуры заголовков
        -Обработка блоков кода и форматирования
        -Построение дерева элементов документа
2.3.5. Фоновый парсинг
    -Алгоритм асинхронной обработки задач парсинга
    -Очередь с приоритетами для управления задачами
    -Механизм выполнения парсинга в отдельном потоке
    -Координация с основным потоком через сигналы Qt
    -Алгоритм обновления интерфейса после завершения парсинга
2.3.6. Алгоритмы модели данных
    2.3.6.1. Построение дерева файлов
        -Алгоритм преобразования результатов парсинга в иерархическую структуру
        -Создание элементов дерева (STMDFileTreeItem)
        -Установка связей родитель-потомок
        -Алгоритм обновления модели при изменениях
    2.3.6.2. Интеграция с Qt Model-View
        -Алгоритм реализации QAbstractItemModel
        -Методы получения данных для отображения (data, index, parent)
        -Алгоритм обновления представления при изменении модели
2.3.7. Примеры работы алгоритмов
    2.3.7.1. Сценарий открытия ST-файла
        -Последовательность вызовов алгоритмов
        -Диаграмма последовательности операций
        -Вырезки кода ключевых алгоритмов
    2.3.7.2. Сценарий кэширования и инвалидации
        -Алгоритм проверки кэша при открытии файла
        -Алгоритм инвалидации при изменении файла
        -Синхронизация между ContentCache и MetadataCache